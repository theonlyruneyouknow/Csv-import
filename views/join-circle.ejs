<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Join Family Circle</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .invitation-card {
            background: white;
            border-radius: 20px;
            padding: 50px 40px;
            max-width: 500px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.2);
            text-align: center;
        }
        .invitation-icon {
            font-size: 5rem;
            color: #667eea;
            margin-bottom: 20px;
        }
        .invitation-card h1 {
            color: #333;
            font-weight: 700;
            margin-bottom: 15px;
        }
        .circle-name {
            color: #667eea;
            font-size: 1.8rem;
            font-weight: 700;
            margin-bottom: 20px;
        }
        .invitation-card p {
            color: #666;
            font-size: 1.1rem;
            margin-bottom: 30px;
        }
        .btn-accept {
            background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
            color: white;
            border: none;
            padding: 15px 40px;
            font-size: 1.2rem;
            font-weight: 600;
            border-radius: 10px;
            transition: all 0.3s ease;
        }
        .btn-accept:hover {
            background: linear-gradient(135deg, #20c997 0%, #28a745 100%);
            transform: scale(1.05);
            box-shadow: 0 10px 25px rgba(40, 167, 69, 0.4);
        }
        .error-message {
            background: #f8d7da;
            color: #721c24;
            padding: 15px;
            border-radius: 10px;
            margin-bottom: 20px;
        }
        .success-message {
            background: #d4edda;
            color: #155724;
            padding: 15px;
            border-radius: 10px;
            margin-bottom: 20px;
        }
        .invited-by {
            color: #999;
            font-size: 0.9rem;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="invitation-card">
        <% if (error) { %>
            <div class="error-message">
                <i class="fas fa-exclamation-circle"></i> <%= error %>
            </div>
            <a href="/" class="btn btn-secondary">Go to Home</a>
        <% } else if (success) { %>
            <div class="success-message">
                <i class="fas fa-check-circle"></i> <%= success %>
            </div>
            <a href="/greatestjoy/familycircles" class="btn btn-accept">View Your Circles</a>
        <% } else { %>
            <i class="fas fa-users invitation-icon"></i>
            <h1>You've Been Invited!</h1>
            <div class="circle-name"><%= circle.name %></div>
            <% if (circle.description) { %>
                <p><%= circle.description %></p>
            <% } %>
            <p>
                Join this family circle to share precious memories and stay connected with your loved ones.
            </p>
            
            <form action="/greatestjoy/familycircles/join/<%= inviteCode %>" method="POST">
                <button type="submit" class="btn btn-accept">
                    <i class="fas fa-user-plus me-2"></i>Accept & Join Circle
                </button>
            </form>
            
            <% if (invitedBy) { %>
                <div class="invited-by">
                    <i class="fas fa-envelope"></i> Invited by <%= invitedBy %>
                </div>
            <% } %>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Family Circles - Greatest Joy</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <%- include('partials/user-dropdown-styles') %>
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 20px;
            background: white;
            border-bottom: 2px solid #dee2e6;
            position: sticky;
            top: 0;
            z-index: 1000;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }

        .header h2 {
            margin: 0;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .header-actions {
            display: flex;
            gap: 10px;
            align-items: center;
        }

        .user-profile-section {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .user-avatar {
            width: 45px;
            height: 45px;
            border-radius: 50%;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
            cursor: pointer;
            font-size: 0.9rem;
            transition: all 0.3s ease;
        }

        .user-avatar:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(102, 126, 234, 0.3);
        }

        .main-container {
            padding: 30px 20px;
            max-width: 1400px;
            margin: 0 auto;
        }
        .page-header {
            background: white;
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }
        .page-header h1 {
            color: #667eea;
            font-weight: 700;
            margin-bottom: 10px;
        }
        .page-header p {
            color: #666;
            margin: 0;
        }
        .circle-card {
            background: white;
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 20px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.08);
            transition: all 0.3s ease;
        }
        .circle-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0,0,0,0.15);
        }
        .circle-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
        }
        .circle-name {
            color: #667eea;
            font-size: 1.5rem;
            font-weight: 600;
            margin: 0;
        }
        .circle-role {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 600;
        }
        .circle-description {
            color: #666;
            margin-bottom: 15px;
        }
        .circle-stats {
            display: flex;
            gap: 20px;
            padding: 15px 0;
            border-top: 1px solid #eee;
            border-bottom: 1px solid #eee;
            margin-bottom: 15px;
        }
        .stat-item {
            display: flex;
            align-items: center;
            gap: 8px;
            color: #666;
        }
        .stat-item i {
            color: #667eea;
        }
        .circle-actions {
            display: flex;
            gap: 10px;
            justify-content: flex-end;
        }
        .btn-custom {
            border-radius: 8px;
            padding: 8px 20px;
            font-weight: 600;
            text-decoration: none;
            transition: all 0.3s ease;
        }
        .btn-view {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
        }
        .btn-view:hover {
            background: linear-gradient(135deg, #764ba2 0%, #667eea 100%);
            color: white;
            transform: scale(1.05);
        }
        .btn-create {
            background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
            color: white;
            border: none;
        }
        .btn-create:hover {
            background: linear-gradient(135deg, #20c997 0%, #28a745 100%);
            transform: scale(1.05);
        }
        .empty-state {
            background: white;
            border-radius: 15px;
            padding: 60px 30px;
            text-align: center;
            box-shadow: 0 5px 15px rgba(0,0,0,0.08);
        }
        .empty-state i {
            font-size: 4rem;
            color: #ccc;
            margin-bottom: 20px;
        }
        .empty-state h3 {
            color: #666;
            margin-bottom: 10px;
        }
        .empty-state p {
            color: #999;
            margin-bottom: 25px;
        }
        .create-circle-section {
            background: white;
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.08);
        }
        .create-circle-section h2 {
            color: #667eea;
            font-size: 1.8rem;
            margin-bottom: 20px;
        }
        .form-label {
            color: #333;
            font-weight: 600;
            margin-bottom: 8px;
        }
        .form-control, .form-select {
            border-radius: 8px;
            border: 2px solid #e0e0e0;
            padding: 10px 15px;
        }
        .form-control:focus, .form-select:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
        }
    </style>
</head>
<body>
    <!-- Header -->
    <div class="header">
        <div>
            <h2><i class="fas fa-users"></i> Family Circles</h2>
        </div>
        <div class="header-actions">
            <a href="/greatestjoy/dashboard" class="btn btn-outline-secondary">
                <i class="fas fa-arrow-left me-1"></i>Back to Gallery
            </a>
            
            <!-- User Dropdown -->
            <% if (typeof user !== 'undefined' && user) { %>
                <div class="user-profile-section">
                    <%- include('partials/user-dropdown', { user: user }) %>
                </div>
            <% } %>
        </div>
    </div>

    <div class="main-container">
        <div class="page-header">
            <div class="d-flex justify-content-between align-items-center">
                <div>
                    <h1><i class="fas fa-users"></i> Family Circles</h1>
                    <p>Create and manage your family sharing groups</p>
                </div>
                <button class="btn btn-create btn-custom" onclick="showCreateForm()">
                    <i class="fas fa-plus"></i> Create New Circle
                </button>
            </div>
        </div>

        <!-- Create Circle Form (hidden by default) -->
        <div id="createCircleForm" class="create-circle-section" style="display: none; margin-bottom: 30px;">
            <h2><i class="fas fa-plus-circle"></i> Create New Family Circle</h2>
            <form action="/greatestjoy/familycircles" method="POST">
                <div class="mb-3">
                    <label for="name" class="form-label">Circle Name *</label>
                    <input type="text" class="form-control" id="name" name="name" required placeholder="e.g., Smith Family, Grandma's Circle">
                </div>
                <div class="mb-3">
                    <label for="description" class="form-label">Description</label>
                    <textarea class="form-control" id="description" name="description" rows="3" placeholder="Describe this family circle..."></textarea>
                </div>
                <div class="mb-3">
                    <label class="form-label">Settings</label>
                    <div class="form-check">
                        <input class="form-check-input" type="checkbox" id="allowMemberInvites" name="allowMemberInvites">
                        <label class="form-check-label" for="allowMemberInvites">
                            Allow members to invite others (not just admins)
                        </label>
                    </div>
                </div>
                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-create btn-custom">
                        <i class="fas fa-check"></i> Create Circle
                    </button>
                    <button type="button" class="btn btn-secondary btn-custom" onclick="hideCreateForm()">
                        Cancel
                    </button>
                </div>
            </form>
        </div>

        <!-- Circles List -->
        <% if (circles && circles.length > 0) { %>
            <div class="row">
                <% circles.forEach(circle => { %>
                    <div class="col-md-6 col-lg-4">
                        <div class="circle-card">
                            <div class="circle-header">
                                <h3 class="circle-name"><%= circle.name %></h3>
                                <span class="circle-role">
                                    <i class="fas fa-<%= circle.userRole === 'admin' ? 'crown' : 'user' %>"></i>
                                    <%= circle.userRole %>
                                </span>
                            </div>
                            
                            <% if (circle.description) { %>
                                <p class="circle-description"><%= circle.description %></p>
                            <% } %>
                            
                            <div class="circle-stats">
                                <div class="stat-item">
                                    <i class="fas fa-users"></i>
                                    <span><%= circle.members.length %> members</span>
                                </div>
                                <div class="stat-item">
                                    <i class="fas fa-envelope"></i>
                                    <span><%= circle.invitations.filter(i => i.status === 'pending').length %> pending</span>
                                </div>
                            </div>
                            
                            <div class="circle-actions">
                                <a href="/greatestjoy/familycircles/<%= circle._id %>" class="btn btn-view btn-custom">
                                    <i class="fas fa-eye"></i> View Details
                                </a>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <div class="empty-state">
                <i class="fas fa-users-slash"></i>
                <h3>No Family Circles Yet</h3>
                <p>Create your first family circle to start sharing photos and videos with loved ones</p>
                <button class="btn btn-create btn-custom" onclick="showCreateForm()">
                    <i class="fas fa-plus"></i> Create Your First Circle
                </button>
            </div>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        function showCreateForm() {
            document.getElementById('createCircleForm').style.display = 'block';
            document.getElementById('createCircleForm').scrollIntoView({ behavior: 'smooth' });
        }

        function hideCreateForm() {
            document.getElementById('createCircleForm').style.display = 'none';
        }
    </script>
</body>
</html>

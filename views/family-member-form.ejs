<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Medicine Management</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Segoe UI', system-ui, sans-serif;
        }
        
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 2rem 1rem;
        }
        
        .form-card {
            background: white;
            border-radius: 12px;
            padding: 2rem;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            border: 1px solid #e9ecef;
        }
        
        .form-header {
            background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
            color: white;
            padding: 1.5rem 2rem;
            border-radius: 12px 12px 0 0;
            margin: -2rem -2rem 2rem -2rem;
        }
        
        .form-group {
            margin-bottom: 1.5rem;
        }
        
        .form-label {
            font-weight: 500;
            margin-bottom: 0.5rem;
            color: #495057;
        }
        
        .form-control, .form-select {
            border-radius: 8px;
            border: 2px solid #e9ecef;
            padding: 0.75rem 1rem;
            transition: all 0.3s ease;
        }
        
        .form-control:focus, .form-select:focus {
            border-color: #28a745;
            box-shadow: 0 0 0 0.2rem rgba(40, 167, 69, 0.25);
        }
        
        .btn-primary {
            background: #28a745;
            border-color: #28a745;
            padding: 0.75rem 2rem;
            border-radius: 8px;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        
        .btn-primary:hover {
            background: #218838;
            border-color: #1e7e34;
            transform: translateY(-1px);
        }
        
        .btn-secondary {
            padding: 0.75rem 2rem;
            border-radius: 8px;
            font-weight: 500;
        }
        
        .section-title {
            color: #495057;
            font-weight: 600;
            margin-bottom: 1rem;
            border-bottom: 2px solid #dee2e6;
            padding-bottom: 0.5rem;
        }
        
        .required {
            color: #dc3545;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Back Button -->
        <div class="mb-3">
            <a href="/medicine/family" class="btn btn-outline-secondary">
                <i class="fas fa-arrow-left me-2"></i>Back to Family Members
            </a>
        </div>

        <div class="form-card">
            <div class="form-header">
                <h1 class="h3 mb-0">
                    <i class="fas fa-user-plus me-3"></i><%= title %>
                </h1>
                <p class="mb-0 mt-2 opacity-75">Add a family member to track their medications</p>
            </div>

            <% if (typeof error !== 'undefined' && error) { %>
                <div class="alert alert-danger">
                    <i class="fas fa-exclamation-triangle me-2"></i><%= error %>
                </div>
            <% } %>

            <form action="<%= isEdit ? '/medicine/family/' + familyMember._id + '?_method=PUT' : '/medicine/family' %>" method="POST">
                <% if (isEdit) { %>
                    <input type="hidden" name="_method" value="PUT">
                <% } %>

                <!-- Basic Information -->
                <div class="section-title">
                    <i class="fas fa-info-circle me-2"></i>Basic Information
                </div>

                <div class="row">
                    <div class="col-md-6">
                        <div class="form-group">
                            <label for="firstName" class="form-label">First Name <span class="required">*</span></label>
                            <input type="text" class="form-control" id="firstName" name="firstName" required 
                                   value="<%= familyMember ? familyMember.firstName : '' %>">
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="form-group">
                            <label for="lastName" class="form-label">Last Name <span class="required">*</span></label>
                            <input type="text" class="form-control" id="lastName" name="lastName" required 
                                   value="<%= familyMember ? familyMember.lastName : '' %>">
                        </div>
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-6">
                        <div class="form-group">
                            <label for="relationship" class="form-label">Relationship <span class="required">*</span></label>
                            <select class="form-select" id="relationship" name="relationship" required>
                                <option value="">Select relationship...</option>
                                <option value="self" <%= familyMember && familyMember.relationship === 'self' ? 'selected' : '' %>>Self (You)</option>
                                <option value="spouse" <%= familyMember && familyMember.relationship === 'spouse' ? 'selected' : '' %>>Spouse</option>
                                <option value="partner" <%= familyMember && familyMember.relationship === 'partner' ? 'selected' : '' %>>Partner</option>
                                <option value="child" <%= familyMember && familyMember.relationship === 'child' ? 'selected' : '' %>>Child</option>
                                <option value="parent" <%= familyMember && familyMember.relationship === 'parent' ? 'selected' : '' %>>Parent</option>
                                <option value="sibling" <%= familyMember && familyMember.relationship === 'sibling' ? 'selected' : '' %>>Sibling</option>
                                <option value="grandparent" <%= familyMember && familyMember.relationship === 'grandparent' ? 'selected' : '' %>>Grandparent</option>
                                <option value="grandchild" <%= familyMember && familyMember.relationship === 'grandchild' ? 'selected' : '' %>>Grandchild</option>
                                <option value="other" <%= familyMember && familyMember.relationship === 'other' ? 'selected' : '' %>>Other</option>
                            </select>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="form-group">
                            <label for="dateOfBirth" class="form-label">Date of Birth</label>
                            <input type="date" class="form-control" id="dateOfBirth" name="dateOfBirth" 
                                   value="<%= familyMember && familyMember.dateOfBirth ? familyMember.dateOfBirth.toISOString().split('T')[0] : '' %>">
                        </div>
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-6">
                        <div class="form-group">
                            <label for="gender" class="form-label">Gender</label>
                            <select class="form-select" id="gender" name="gender">
                                <option value="prefer-not-to-say" <%= !familyMember || familyMember.gender === 'prefer-not-to-say' ? 'selected' : '' %>>Prefer not to say</option>
                                <option value="male" <%= familyMember && familyMember.gender === 'male' ? 'selected' : '' %>>Male</option>
                                <option value="female" <%= familyMember && familyMember.gender === 'female' ? 'selected' : '' %>>Female</option>
                                <option value="other" <%= familyMember && familyMember.gender === 'other' ? 'selected' : '' %>>Other</option>
                            </select>
                        </div>
                    </div>
                </div>

                <!-- Contact Information -->
                <div class="section-title mt-4">
                    <i class="fas fa-address-book me-2"></i>Contact Information
                </div>

                <div class="row">
                    <div class="col-md-6">
                        <div class="form-group">
                            <label for="email" class="form-label">Email</label>
                            <input type="email" class="form-control" id="email" name="email" 
                                   value="<%= familyMember ? familyMember.email : '' %>">
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="form-group">
                            <label for="phone" class="form-label">Phone Number</label>
                            <input type="tel" class="form-control" id="phone" name="phone" 
                                   value="<%= familyMember ? familyMember.phone : '' %>">
                        </div>
                    </div>
                </div>

                <!-- Medical Information -->
                <div class="section-title mt-4">
                    <i class="fas fa-stethoscope me-2"></i>Medical Information (Optional)
                </div>

                <div class="form-group">
                    <label for="allergies" class="form-label">Known Allergies</label>
                    <textarea class="form-control" id="allergies" name="allergies" rows="2" 
                              placeholder="List any known allergies (one per line)"><%= familyMember && familyMember.allergies ? familyMember.allergies.map(a => a.allergen).join('\n') : '' %></textarea>
                    <small class="form-text text-muted">Enter each allergy on a separate line</small>
                </div>

                <div class="form-group">
                    <label for="medicalConditions" class="form-label">Medical Conditions</label>
                    <textarea class="form-control" id="medicalConditions" name="medicalConditions" rows="2" 
                              placeholder="List any medical conditions (one per line)"><%= familyMember && familyMember.medicalConditions ? familyMember.medicalConditions.map(c => c.condition).join('\n') : '' %></textarea>
                    <small class="form-text text-muted">Enter each condition on a separate line</small>
                </div>

                <div class="form-group">
                    <label for="notes" class="form-label">Additional Notes</label>
                    <textarea class="form-control" id="notes" name="notes" rows="3" 
                              placeholder="Any additional information about this family member..."><%= familyMember ? familyMember.notes : '' %></textarea>
                </div>

                <!-- Form Actions -->
                <div class="d-flex justify-content-between align-items-center mt-4 pt-3 border-top">
                    <a href="/medicine/family" class="btn btn-secondary">
                        <i class="fas fa-times me-2"></i>Cancel
                    </a>
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-save me-2"></i><%= isEdit ? 'Update' : 'Add' %> Family Member
                    </button>
                </div>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
